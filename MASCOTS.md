# 마스코트 팩 만들기 (Rondo)

Rondo 의 마스코트는 **팩(pack)** 하나 = 폴더 하나입니다. 규격만 맞추면 누구나 만들 수 있고,
Rondo 에 들어 있는 팩 8종은 전부 **CC0** 입니다 — 뜯어보고 베끼고 고쳐 쓰세요.

> 이 문서는 두 가지를 설명합니다: **① 규격**(무엇을 맞춰야 하는가)과
> **② 만드는 방법**(직접 찍든, 생성 모델로 그리든).

---

## 1. 폴더 하나가 팩 하나

```
<팩이름>/
  idle.png  blink.png  wait.png  look-l.png  look-r.png
  work.png  ask.png    perm.png  done.png    happy.png
  greet.png sleep.png  error.png alert.png     ← 상태 14컷 (전부 필요)
  front.png front-blink.png                    ← 정면 2컷 (선택)
```

- **파일 이름이 곧 상태 이름입니다.** 하나라도 빠지면 **그 상태일 때 마스코트가 사라집니다** —
  가장 안 들키는 종류의 결함입니다(오류 컷이 없으면 오류가 났을 때만 사라지니까요).
- `front.png` 는 **멈춰서 쉴 때** 씁니다. 없으면 늘 옆모습입니다.

| 상태 | 언제 |
|---|---|
| `idle` `blink` `wait` | 아무 일도 없을 때 (쉬는 컷 — 정면으로 돌아보는 건 이 셋뿐입니다) |
| `look-l` `look-r` | 유휴 중 두리번거림 |
| `work` | 턴이 도는 중 |
| `ask` `perm` | 질문 대기 · 권한 대기 |
| `done` `happy` `greet` | 완료 · 기쁨 · 인사 |
| `sleep` | 오래 유휴 |
| `error` `alert` | 오류 · 경고 |

## 2. 그림 규격

| 항목 | 값 |
|---|---|
| 캔버스 | **768 × 640 px** (= 24칸 × 20칸, 한 칸 32px) |
| 몸통 | **아래쪽 16칸 × 10칸** 안 (가로 4~19 · 세로 10~19) |
| 위 10줄 | **부속 전용** (zzz · ❗ · ✨ · ♥ · ✕) — 몸통은 올라오지 않습니다 |
| 배경 | 완전 투명 |
| 색 | 캐릭터 **5~6색** + 부속 공용색 |
| 안티에일리어싱 | **없음** — 한 칸은 단색으로 꽉 차거나 완전히 빕니다 |

**세 가지가 특히 중요합니다.**

1. **바닥선을 고정하세요.** 마지막으로 칠해진 칸의 y 가 14컷 모두 같아야 합니다.
   안 그러면 상태가 바뀔 때마다 **발이 위아래로 튑니다.**
2. **몸통 크기를 컷마다 바꾸지 마세요.** 부속(zzz)이 있는 컷에서 몸을 줄이면 상태 전환 때마다
   캐릭터가 커졌다 작아집니다. 부속은 **위 여백**에 그리는 것이지 몸을 밀어내는 게 아닙니다.
3. **부속은 최소 2칸 × 2칸.** 1칸짜리 반짝임은 실제 표시 크기(34px)에서 **아예 안 보입니다** — 실측입니다.

### 시점 — 옆모습으로 그리세요

동물은 **왼쪽을 보는 옆모습**이 기본입니다.
- 정면에서 네발 동물을 16×10 으로 누르면 얼굴만 남고 몸이 사라져 **깔개처럼** 보입니다.
  옆모습이면 같은 칸 수로 귀·등선·네 다리·꼬리가 전부 삽니다.
- 말풍선이 마스코트 **왼쪽**에 뜨므로, 왼쪽을 봐야 자기 말풍선 쪽을 향합니다.
- **오른쪽으로 걸을 땐 Rondo 가 좌우로 뒤집어 씁니다** — 오른쪽 컷을 따로 그릴 필요가 없습니다.

## 3. 만드는 방법 — 한 컷을 그리고 나머지는 **계산합니다**

⛔ 14컷을 하나씩 그리지 마세요. 컷마다 새로 그리면 다리 길이·등선·무늬가 조금씩 흘러가
**상태가 바뀔 때마다 다른 동물로 갈아끼우는 것처럼** 보입니다. Rondo 가 쓰는 방법은 이렇습니다:

```
① 그림 2장  ──────  idle(옆모습) · front(정면)
      ↓            생성 모델로 그려도 되고 직접 찍어도 됩니다
② 격자에 앉히기 ──  scripts/mascot-pixelize  (흰/키 배경 제거 · 24×20 스냅 · 칸마다 최빈색 · 팔레트 축소)
      ↓
③ 나머지 14컷 ───  scripts/mascot-compose  (눈 감기기 · 귀 접기 · 머리 위 기호 얹기)
```

```bash
# ② 그림 → 격자 (PNG + diff 가능한 격자 JSON)
npx electron scripts/mascot-pixelize raw/idle.png out/idle.png --json out/idle.json --snap --colors 6

# ③ 격자 → 16컷
npx electron scripts/mascot-compose  src/<팩>/char.json  <출력폴더>
```

`char.json` 은 **비워 두어도 됩니다** — 눈·귀·앞다리 위치를 그림에서 자동으로 찾습니다.
결과가 어색하면 그 값만 적어서 덮어쓰세요.

```jsonc
{
  "id": "my-cat",
  "base": "base.json",                       // ② 가 만든 격자 JSON
  "front": { "base": "front-base.json" },
  // ↓ 아래는 전부 선택 — 적으면 자동 추론을 이깁니다
  "eye": { "x": 5, "y": 12, "h": 2 },
  "ears": [[4, 10], [6, 10]],
  "acc": { "cx": 11, "bottom": 8 }            // 기호가 뜨는 자리
}
```

### 생성 모델에게 시킬 때 — 실제로 겪은 함정 셋

1. **"투명 배경" 이라고 해도 흰색으로 칠해 옵니다.** 그건 괜찮습니다(스크립트가 걷어냅니다).
   문제는 **흰 캐릭터**입니다 — 말티즈를 흰 배경에 그렸더니 480칸 중 20칸만 남았습니다.
   → 밝은 캐릭터는 **배경을 마젠타(`#FF00FF`)로** 지정하세요. 캐릭터가 쓸 리 없는 색이라 안전합니다.
2. **속이 빈 도넛이 나옵니다.** 모델이 윤곽선만 그리는 경우가 있습니다 —
   프롬프트에 *"실루엣 안의 모든 칸을 단색으로 꽉 채우세요"* 를 **명시**하세요.
3. **"작은 동물이니 작게" 라고 하면 안 됩니다.** 햄스터도 상자를 꽉 채워야 합니다.
   크기가 팩마다 다르면 갈아끼울 때 화면 무게중심이 흔들립니다 — 작다는 건 **비율**로 표현하세요.

## 4. 넣기

만든 폴더를 Rondo 의 마스코트 폴더에 넣고 **설정 → 일반 → 마스코트**에서 고릅니다.
(현재 버전은 내장 팩만 목록에 뜹니다 — 사용자 폴더 팩 로딩은 다음 업데이트입니다.)

## 5. 라이선스

Rondo 내장 팩 8종(Dot · 길고양이 · 시베리안 고양이 · 시바견 · 말티즈 · 햄스터 · 문조 · 수달)은
**CC0-1.0** 입니다. 저작권 표시 없이 자유롭게 쓰고, 고치고, 재배포해도 됩니다.
